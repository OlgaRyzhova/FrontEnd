"use strict";

$(function () {
  $('.slider_block').slick({
    // lazyLoad: 'ondemand',
    prevArrow: false,
    nextArrow: false,
    autoplay: true,
    autoplaySpeed: 1500,
    dots: false
  });
});